# AuditLLM 🛡️

An offline reliability and testing harness designed to audit downstream LLM outputs and AI agents for structural compliance, performance SLAs, and semantic hallucination rates.

## Why AuditLLM?
Most AI engineering focuses on generation (wrappers, chat UIs). **AuditLLM** focuses on **testing and reliability infrastructure**—acting as a CI/CD quality gate to catch silent regressions, invalid JSON schemas, and ungrounded hallucinations before code reaches production.

## Quick Start

**1. Install dependencies**
```bash
pip install pydantic rich
```

**2. Run the default audit suite**
```bash
python main.py
```
This runs the built-in 3-case suite (a clean extraction, a hallucination trap, and a malformed-JSON case) against the mock target agent and writes the results to `reports/audit_report.md`.

**3. Run your own test suite**
```bash
python main.py --config custom_tests.json --output reports/custom_audit_report.md
```
Point `--config` at any JSON file containing a list of `{name, prompt, source_context}` objects to audit your own cases, and `--output` at wherever you want the report written.

## Example Output

Every run produces a clean, minimalist Markdown report. Here's what the default suite catches — one clean pass, one caught hallucination, and one broken schema:

```markdown
# Audit Report

* Timestamp: 2026-09-18 15:54:01 UTC
* Summary: 1/3 Passed (33.3%)

---
### standard_valid_extraction [PASS]

- Schema: PASS
- Latency: PASS (0.0s)
- Semantic: PASS
  - Rationale: RATIONALE: Claim is fully supported by source context.

### hallucination_test_case [FAIL]

- Schema: PASS
- Latency: PASS (0.0s)
- Semantic: FAIL
  - Rationale: RATIONALE: Local judge detected unauthorized/ungrounded claims in text: 'The patient was prescribed 500mg of Penicillin and diagnosed with acute bronchitis.'

### structural_break_test [FAIL]

- Schema: FAIL (2 validation errors for AgentOutputSchema
summary
  Field required [type=missing, input_value={'raw_output': 'Here is y... John Doe, age: thirty'}, input_type=dict]
    For further information visit https://errors.pydantic.dev/2.13/v/missing
confidence
  Field required [type=missing, input_value={'raw_output': 'Here is y... John Doe, age: thirty'}, input_type=dict]
    For further information visit https://errors.pydantic.dev/2.13/v/missing)
- Latency: PASS (0.0s)
- Semantic: PASS
  - Rationale: Skipped due to schema failure

---
Generated by AuditLLM.
```

Pointing `--config` at a custom suite (like `custom_tests.json`) produces the same report shape for your own cases:

```markdown
# Audit Report

* Timestamp: 2026-09-18 15:55:08 UTC
* Summary: 1/2 Passed (50.0%)

---
### custom_valid_extraction [PASS]

- Schema: PASS
- Latency: PASS (0.0s)
- Semantic: PASS
  - Rationale: RATIONALE: Claim is fully supported by source context.

### custom_hallucination_check [FAIL]

- Schema: PASS
- Latency: PASS (0.0s)
- Semantic: FAIL
  - Rationale: RATIONALE: Local judge detected unauthorized/ungrounded claims in text: 'The patient was prescribed 500mg of Penicillin and diagnosed with acute bronchitis.'

---
Generated by AuditLLM.
```

## Architecture
AuditLLM uses a dual-layer evaluation pipeline:
1. **Deterministic Gatekeeper:** Fast, local code-based checks using Pydantic v2 schemas and latency SLA boundaries. If an agent breaks its JSON contract, the engine short-circuits to save resources.
2. **Semantic Judge Layer:** Evaluates agent claims against source contexts to detect hallucinations and consistency failures.
3. **Report Exporter:** Automatically generates clean, minimalist Markdown execution reports for PR/CI integration.

## Project Structure
```text
audit-llm/
│
├── core/
│   ├── checker.py      # Structural & latency validation
│   ├── runner.py       # Orchestration & test pipeline
│   └── reporter.py     # Minimalist Markdown report generator
│
├── judges/
│   └── llm_judge.py    # Semantic heuristic hallucination checks
│
├── examples/
│   └── target_agent.py # Mock buggy target agent for test simulations
│
├── reports/            # Generated audit execution logs
├── main.py             # CLI entrypoint
└── requirements.txt
```
